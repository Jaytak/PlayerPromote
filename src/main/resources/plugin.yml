name: PlayerPromote
version: '0.1.22'
main: com.jaytak.playerPromote.PlayerPromote
api-version: '1.21'
commands:
  agree:
    description: Promotes the player to the "player" group.
    usage: /agree
    permission: playerpromote.use
permissions:
  playerpromote.use:
    description: allows use of promote command.
    default: false
